paddlepaddle==3.0.0   # CPU wheel, from official index (not PyPI default)
paddleocr==3.2.0
paddlex==3.2.0
numpy==2.0.*          # keep consistent with Paddle’s wheel
opencv-python-headless==4.10.*   # use headless instead of contrib (smaller, no GUI)
Pillow==11.0.*
pyclipper==1.3.0.post6
